<!DOCTYPE html>
<html lang="cn">
    <head>
        {{#if title}}
            <title>{{title}}</title>
        {{else}}
            <title>守望先锋</title>
        {{/if}}
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0, minimum-scale=1.0, maximum-scale=1.0">
        <script src="http://apps.bdimg.com/libs/jquery/2.1.4/jquery.min.js" type="text/javascript"></script>
        <style media="screen">
            @font-face {
                font-family: ow_icon;
                src: url(/stylesheets/ow.ttf)
            }
            body {
                background-image: url('{{files.body_back_img}}');
                width: 1200px;
                margin-left: auto;
                margin-right: auto;
                margin-top: 0;
                background-color: #FBFBFB;
            }
            .body {
                position: relative;
                width: 100%;
                color: #fff;
            }
            .top {
                width: 100%;
                height: 300px;
            }
            .top img {
                display: block;
                height: 510px;

                opacity: .8;
            }
            .top .ow-logo {
                display: block;
                position: absolute;
                right: 215px;
                top: 100px;
                height: 150px;

                opacity: 1;
            }

            .box {
                padding: 10px;
                margin-bottom: 20px;
                box-sizing: border-box;

                background: rgba(255, 255, 255, 0.35);
            }

            .box-hd {
                position: relative;
                margin-left: 10px;
                text-align: left;
                border-bottom: 1px solid #EAEAEA;
            }

            .box-hd .title {
                display: inline-block;
                position: relative;
                height: 34px;
                margin: 0;
                font-size: 16px;
                line-height: 34px;

                color: #fff;
                margin-bottom: -1px;
                border-bottom: 2px solid #3a6ecc;
            }

            .left {
                position: absolute;
                left: 0;
                width: 875px;
            }

            ul {
                margin-left: auto;
                margin-right: auto;
                padding-left: 0px;
                text-align: center;
            }

            .maps ul li {
                display: inline-block;
                width: 185px;
                height: 120px;
                margin: 8px;

                overflow: hidden;
                transition: all 0.5s;
            }
            .maps ul li img {
                display: block;
                width: 100%;
                height: 100%;
            }
            .maps ul li:hover {
                transform: translateY(-10px);
            }
            .maps ul li .intro {
                position: relative;
                width: 100%;
                height: 25px;
                bottom: 25px;
            }
            .maps ul li .intro .type {
                float: left;
            }
            .maps ul li .intro .name {
                float: right;
                margin-right: 10px;
            }

            .blogroll ul li {
                display: inline-block;
                width: 19%;
                height: 100px;
            }
            .blogroll ul li img {
                height: 40px;
                margin-top: 5px;
            }

            .heros {
                position: absolute;
                right: 0;
                width: 310px;
                height: auto;
                box-sizing: border-box;
            }

            .heros .box-hd .role {
                display: inline-block;
                position: absolute;
                right: 10px;
                bottom: 0;

                font-size: larger;
                line-height: 2rem;
                font-family: ow_icon;
            }
            .heros .role span {
                margin-right: 5px;
            }
            .heros .role .role-tank:before {
                content: '\e614';
            }

            .heros .role .role-defense:before {
                content: '\e615';
            }

            .heros .role .role-offense:before {
                content: '\e616';
            }

            .heros .role .role-support:before {
                content: '\e617';
            }
            .heros .role .active {
                color: #00c3ff;
            }

            .heros ul li {
                display: inline-block;
                width: 70px;
                height: 85px;
                margin: 4px;

                overflow: hidden;
                border-radius: 5px;
                -webkit-transform: skew(-.25rad);
                transform: skew(-.25rad);
                transition: all 0.5s;
                background-color: #424042;
            }
            .heros ul li img {
                height: 100%;
                margin-left: -12px;
                transform: skew(.25rad);
            }
            .heros ul li .name {
                position: relative;
                width: 100%;
                height: 20px;

                line-height: 20px;
                font-style: italic;
                font-weight: 100;
                font-size: small;

                border-radius: 5px;
                background-color: #000;
                opacity: 0.7;
                transition: all 0.5s;
            }
            .heros ul li:hover {
                transform: scale(1.2) skew(-.25rad);
                background-color: #6D7F1B;
            }
            .heros ul li:hover >.name {
                margin-top: -20px;
            }
        </style>
    </head>
    <body>
        <div class="body">
            <div class="top">
                <img src="{{files.index_top_img}}" alt="" />
                <img class="ow-logo" src="/images/overwatch-logo.png" alt="" />
            </div>
        {{{ body }}}
        </div>
    </body>
</html>
